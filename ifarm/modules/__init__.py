"""iFarm capability modules — independently importable.

Each module addresses one capability area:
    proxy    — cellular IP routing and rotation
    sms      — SMS / 2FA interception from chat.db
    scraper  — Appium + VLM visual scraping pipeline
    hardware — GPS spoofing and camera injection
"""
