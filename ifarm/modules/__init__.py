"""iFarm capability modules — independently importable.

Each module addresses one capability area:
    proxy    — Phase 1: cellular IP routing and rotation
    sms      — Phase 1: SMS / 2FA interception from chat.db
    scraper  — Phase 2: Appium + VLM visual scraping pipeline
    hardware — Phase 3: GPS spoofing and camera injection
"""
